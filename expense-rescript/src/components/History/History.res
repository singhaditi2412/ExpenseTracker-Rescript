// %%raw("import './History.css'")
// @react.component
// let make = () => {
//   let {addTransactionHandler, transactions} = React.useContext(GlobalContext.context)
//   let sign = (acc, value: GlobalContext.transaction) => value.price < 0 ? "-" : "+"

//   <li className={transaction.price < 0 ? "negative" : "positive"}>
//     {transaction.text}
//     <span>
//       {sign}
//       {React.string("$")(Math.abs(transaction.price))}
//     </span>
//   </li>
// }
